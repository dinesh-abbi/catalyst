const { execSync } = require('child_process');

console.log("Make sure the app is running in Expo!");
console.log("You can trigger notifications directly through an expo-cli extension if you wish, but the app handles it inherently via the router once clicked.");

